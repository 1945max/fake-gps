package id.xxx.fake.test.domain

import org.junit.Assert
import org.junit.Test

class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        val text = " test"

        Assert.assertTrue(text.isNotBlank())
    }
}